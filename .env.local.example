ANTHROPIC_API_KEY=sk-ant-...

# OpenAI (Whisper transcription + gpt-image-1 dream art). Optional — voice/share features degrade gracefully if missing.
OPENAI_API_KEY=

# Neon Postgres
DATABASE_URL=

# Better Auth — generate secret with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=http://localhost:3000
